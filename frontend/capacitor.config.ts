import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.storekit.fr',
  appName: 'storekit',
  webDir: 'dist/storekit/browser',
};

export default config;
